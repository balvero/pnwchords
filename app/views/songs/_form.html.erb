<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.hidden_field 'user_id', :value => @user_id, class: 'form-control' %>
    </div>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :song_title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :key %>
    <%= form.text_field :key, id: :song_key, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, id: :song_body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.collection_select :artist_id, Artist.order(:name), :id, :name, include_blank: true, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>
