<h1>Editing <%= @artist.name %></h1>

<%= render 'form', artist: @artist %>
<hr />
<%= link_to 'Show', @artist, class: 'btn btn-secondary' %>
<%= link_to 'Back', artists_path, class: 'btn btn-secondary' %>
